<Page
    x:Class="CroquisPlayer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CroquisPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <SplitView IsPaneOpen="True" PanePlacement="Right">
            <RelativePanel>
                
            </RelativePanel>
            
            <SplitView.Pane>
                <StackPanel Name="SettingPanel" Orientation="Vertical" Margin="10">
                    <TextBlock Text="Time Interval"/>
                    <Slider/>
                    
                    <TextBlock Text="Break Interval" Margin="0,10,0,0"/>
                    <Slider/>

                    <Button Content="Start" Background="Green" Width="{Binding ElementName=SettingPanel, Path=ActualWidth}"/>
                </StackPanel>
            </SplitView.Pane>
        </SplitView>
    </Grid>
</Page>
